# Cline Multi-Agent Memory Isolation Rule

Use Multi-Agent Memory Isolation to avoid stale task memory:

```bash
agent-memory-isolation handoff --agent cline --task "<task>"
agent-memory-isolation check
```

Private notes stay private. Shared memory requires evidence.
