# OpenCode Multi-Agent Memory Isolation Rule

Run:

```bash
agent-memory-isolation handoff --agent opencode --task "<task>"
```

Use the shared truth section as context. Do not import draft notes from other agents unless they were promoted with evidence.
