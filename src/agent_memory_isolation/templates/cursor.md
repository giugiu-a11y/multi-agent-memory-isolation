# Cursor Multi-Agent Memory Isolation Rule

Keep `.agent-memory/handoffs/latest.md` open when continuing agent work.

Use promoted shared memory as context. Treat agent-local notes as private scratch unless they were promoted with evidence.
